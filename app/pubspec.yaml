name: stock_monitor
description: "Stock Monitor — client Flutter (web + Android) per l'API FastAPI."
publish_to: 'none'
version: 4.0.0+1

environment:
  sdk: ^3.13.2

dependencies:
  cupertino_icons: ^1.0.9
  dio: ^5.11.1
  file_picker: ^12.3.0
  file_saver: ^0.4.0
  fl_chart: ^1.2.0
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^3.4.3
  flutter_secure_storage: ^11.1.1
  go_router: ^18.0.1
  intl: ^0.20.3
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
